/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getDiscriminatedUnionDecoder,
  getDiscriminatedUnionEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type GetDiscriminatedUnionVariant,
  type GetDiscriminatedUnionVariantContent,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getTargetChainDecoder,
  getTargetChainEncoder,
  type TargetChain,
  type TargetChainArgs,
} from ".";

/**
 * Where milestone payments land for this bid.
 *
 * V1 only `Solana { mint }` is implemented. `CrossChain` is reserved for the
 * Day 10 Ika integration - `select_bid` rejects it with `CrossChainNotYetSupported`
 * for now, so the on-chain shape is forward-compat without breaking changes.
 */
export type PayoutChain =
  | { __kind: "Solana"; mint: Address }
  | {
      __kind: "CrossChain";
      ikaDwalletId: ReadonlyUint8Array;
      targetChain: TargetChain;
    };

export type PayoutChainArgs =
  | { __kind: "Solana"; mint: Address }
  | {
      __kind: "CrossChain";
      ikaDwalletId: ReadonlyUint8Array;
      targetChain: TargetChainArgs;
    };

export function getPayoutChainEncoder(): Encoder<PayoutChainArgs> {
  return getDiscriminatedUnionEncoder([
    ["Solana", getStructEncoder([["mint", getAddressEncoder()]])],
    [
      "CrossChain",
      getStructEncoder([
        ["ikaDwalletId", fixEncoderSize(getBytesEncoder(), 32)],
        ["targetChain", getTargetChainEncoder()],
      ]),
    ],
  ]);
}

export function getPayoutChainDecoder(): Decoder<PayoutChain> {
  return getDiscriminatedUnionDecoder([
    ["Solana", getStructDecoder([["mint", getAddressDecoder()]])],
    [
      "CrossChain",
      getStructDecoder([
        ["ikaDwalletId", fixDecoderSize(getBytesDecoder(), 32)],
        ["targetChain", getTargetChainDecoder()],
      ]),
    ],
  ]);
}

export function getPayoutChainCodec(): Codec<PayoutChainArgs, PayoutChain> {
  return combineCodec(getPayoutChainEncoder(), getPayoutChainDecoder());
}

// Data Enum Helpers.
export function payoutChain(
  kind: "Solana",
  data: GetDiscriminatedUnionVariantContent<
    PayoutChainArgs,
    "__kind",
    "Solana"
  >,
): GetDiscriminatedUnionVariant<PayoutChainArgs, "__kind", "Solana">;
export function payoutChain(
  kind: "CrossChain",
  data: GetDiscriminatedUnionVariantContent<
    PayoutChainArgs,
    "__kind",
    "CrossChain"
  >,
): GetDiscriminatedUnionVariant<PayoutChainArgs, "__kind", "CrossChain">;
export function payoutChain<K extends PayoutChainArgs["__kind"], Data>(
  kind: K,
  data?: Data,
) {
  return Array.isArray(data)
    ? { __kind: kind, fields: data }
    : { __kind: kind, ...(data ?? {}) };
}

export function isPayoutChain<K extends PayoutChain["__kind"]>(
  kind: K,
  value: PayoutChain,
): value is PayoutChain & { __kind: K } {
  return value.__kind === kind;
}
