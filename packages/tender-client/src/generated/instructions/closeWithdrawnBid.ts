/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPermissionPda } from "../pdas";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_WITHDRAWN_BID_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([66, 54, 100, 88, 135, 48, 115, 14]);

export function getCloseWithdrawnBidDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_WITHDRAWN_BID_DISCRIMINATOR,
  );
}

export type CloseWithdrawnBidInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountRfp extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> =
    "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableSignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountRfp extends string ? WritableAccount<TAccountRfp> : TAccountRfp,
      TAccountBid extends string ? WritableAccount<TAccountBid> : TAccountBid,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseWithdrawnBidInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseWithdrawnBidInstructionDataArgs = {};

export function getCloseWithdrawnBidInstructionDataEncoder(): FixedSizeEncoder<CloseWithdrawnBidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_WITHDRAWN_BID_DISCRIMINATOR }),
  );
}

export function getCloseWithdrawnBidInstructionDataDecoder(): FixedSizeDecoder<CloseWithdrawnBidInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseWithdrawnBidInstructionDataCodec(): FixedSizeCodec<
  CloseWithdrawnBidInstructionDataArgs,
  CloseWithdrawnBidInstructionData
> {
  return combineCodec(
    getCloseWithdrawnBidInstructionDataEncoder(),
    getCloseWithdrawnBidInstructionDataDecoder(),
  );
}

export type CloseWithdrawnBidAsyncInput<
  TAccountProvider extends string = string,
  TAccountRfp extends string = string,
  TAccountBid extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  rfp: Address<TAccountRfp>;
  bid: Address<TAccountBid>;
  /** Closed via CPI below to refund the rent (~0.0048 SOL) to the provider. */
  permission?: Address<TAccountPermission>;
  permissionProgram?: Address<TAccountPermissionProgram>;
};

export async function getCloseWithdrawnBidInstructionAsync<
  TAccountProvider extends string,
  TAccountRfp extends string,
  TAccountBid extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: CloseWithdrawnBidAsyncInput<
    TAccountProvider,
    TAccountRfp,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseWithdrawnBidInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountRfp,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    rfp: { value: input.rfp ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.permission.value) {
    accounts.permission.value = await findPermissionPda({
      bid: getAddressFromResolvedInstructionAccount("bid", accounts.bid.value),
    });
  }
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("rfp", accounts.rfp),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("permissionProgram", accounts.permissionProgram),
    ],
    data: getCloseWithdrawnBidInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseWithdrawnBidInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountRfp,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type CloseWithdrawnBidInput<
  TAccountProvider extends string = string,
  TAccountRfp extends string = string,
  TAccountBid extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  rfp: Address<TAccountRfp>;
  bid: Address<TAccountBid>;
  /** Closed via CPI below to refund the rent (~0.0048 SOL) to the provider. */
  permission: Address<TAccountPermission>;
  permissionProgram?: Address<TAccountPermissionProgram>;
};

export function getCloseWithdrawnBidInstruction<
  TAccountProvider extends string,
  TAccountRfp extends string,
  TAccountBid extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: CloseWithdrawnBidInput<
    TAccountProvider,
    TAccountRfp,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseWithdrawnBidInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountRfp,
  TAccountBid,
  TAccountPermission,
  TAccountPermissionProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    rfp: { value: input.rfp ?? null, isWritable: true },
    bid: { value: input.bid ?? null, isWritable: true },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("rfp", accounts.rfp),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("permissionProgram", accounts.permissionProgram),
    ],
    data: getCloseWithdrawnBidInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseWithdrawnBidInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountRfp,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type ParsedCloseWithdrawnBidInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    rfp: TAccountMetas[1];
    bid: TAccountMetas[2];
    /** Closed via CPI below to refund the rent (~0.0048 SOL) to the provider. */
    permission: TAccountMetas[3];
    permissionProgram: TAccountMetas[4];
  };
  data: CloseWithdrawnBidInstructionData;
};

export function parseCloseWithdrawnBidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseWithdrawnBidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      provider: getNextAccount(),
      rfp: getNextAccount(),
      bid: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
    },
    data: getCloseWithdrawnBidInstructionDataDecoder().decode(instruction.data),
  };
}
