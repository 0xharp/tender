/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findPermissionPda } from "../pdas";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const OPEN_REVEAL_WINDOW_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([11, 52, 255, 148, 71, 150, 141, 209]);

export function getOpenRevealWindowDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    OPEN_REVEAL_WINDOW_DISCRIMINATOR,
  );
}

export type OpenRevealWindowInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TAccountPermission extends string | AccountMeta<string> = string,
  TAccountPermissionProgram extends string | AccountMeta<string> =
    "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountBid extends string ? ReadonlyAccount<TAccountBid> : TAccountBid,
      TAccountPermission extends string
        ? WritableAccount<TAccountPermission>
        : TAccountPermission,
      TAccountPermissionProgram extends string
        ? ReadonlyAccount<TAccountPermissionProgram>
        : TAccountPermissionProgram,
      ...TRemainingAccounts,
    ]
  >;

export type OpenRevealWindowInstructionData = {
  discriminator: ReadonlyUint8Array;
  /**
   * The provider's wallet pubkey. Verified against `bid.provider_identity`
   * before being included in the new permission set.
   */
  providerWallet: Address;
};

export type OpenRevealWindowInstructionDataArgs = {
  /**
   * The provider's wallet pubkey. Verified against `bid.provider_identity`
   * before being included in the new permission set.
   */
  providerWallet: Address;
};

export function getOpenRevealWindowInstructionDataEncoder(): FixedSizeEncoder<OpenRevealWindowInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["providerWallet", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: OPEN_REVEAL_WINDOW_DISCRIMINATOR }),
  );
}

export function getOpenRevealWindowInstructionDataDecoder(): FixedSizeDecoder<OpenRevealWindowInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["providerWallet", getAddressDecoder()],
  ]);
}

export function getOpenRevealWindowInstructionDataCodec(): FixedSizeCodec<
  OpenRevealWindowInstructionDataArgs,
  OpenRevealWindowInstructionData
> {
  return combineCodec(
    getOpenRevealWindowInstructionDataEncoder(),
    getOpenRevealWindowInstructionDataDecoder(),
  );
}

export type OpenRevealWindowAsyncInput<
  TAccountPayer extends string = string,
  TAccountBid extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  /** Anyone — this ix is permissionless once the time gate is satisfied. */
  payer: TransactionSigner<TAccountPayer>;
  bid: Address<TAccountBid>;
  permission?: Address<TAccountPermission>;
  permissionProgram?: Address<TAccountPermissionProgram>;
  providerWallet: OpenRevealWindowInstructionDataArgs["providerWallet"];
};

export async function getOpenRevealWindowInstructionAsync<
  TAccountPayer extends string,
  TAccountBid extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: OpenRevealWindowAsyncInput<
    TAccountPayer,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  OpenRevealWindowInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    bid: { value: input.bid ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.permission.value) {
    accounts.permission.value = await findPermissionPda({
      bid: getAddressFromResolvedInstructionAccount("bid", accounts.bid.value),
    });
  }
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("permissionProgram", accounts.permissionProgram),
    ],
    data: getOpenRevealWindowInstructionDataEncoder().encode(
      args as OpenRevealWindowInstructionDataArgs,
    ),
    programAddress,
  } as OpenRevealWindowInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type OpenRevealWindowInput<
  TAccountPayer extends string = string,
  TAccountBid extends string = string,
  TAccountPermission extends string = string,
  TAccountPermissionProgram extends string = string,
> = {
  /** Anyone — this ix is permissionless once the time gate is satisfied. */
  payer: TransactionSigner<TAccountPayer>;
  bid: Address<TAccountBid>;
  permission: Address<TAccountPermission>;
  permissionProgram?: Address<TAccountPermissionProgram>;
  providerWallet: OpenRevealWindowInstructionDataArgs["providerWallet"];
};

export function getOpenRevealWindowInstruction<
  TAccountPayer extends string,
  TAccountBid extends string,
  TAccountPermission extends string,
  TAccountPermissionProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: OpenRevealWindowInput<
    TAccountPayer,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >,
  config?: { programAddress?: TProgramAddress },
): OpenRevealWindowInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountBid,
  TAccountPermission,
  TAccountPermissionProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    bid: { value: input.bid ?? null, isWritable: false },
    permission: { value: input.permission ?? null, isWritable: true },
    permissionProgram: {
      value: input.permissionProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.permissionProgram.value) {
    accounts.permissionProgram.value =
      "ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1" as Address<"ACLseoPoyC3cBqoUtkbjZ4aDrkurZW86v19pXz2XQnp1">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("bid", accounts.bid),
      getAccountMeta("permission", accounts.permission),
      getAccountMeta("permissionProgram", accounts.permissionProgram),
    ],
    data: getOpenRevealWindowInstructionDataEncoder().encode(
      args as OpenRevealWindowInstructionDataArgs,
    ),
    programAddress,
  } as OpenRevealWindowInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountBid,
    TAccountPermission,
    TAccountPermissionProgram
  >);
}

export type ParsedOpenRevealWindowInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone — this ix is permissionless once the time gate is satisfied. */
    payer: TAccountMetas[0];
    bid: TAccountMetas[1];
    permission: TAccountMetas[2];
    permissionProgram: TAccountMetas[3];
  };
  data: OpenRevealWindowInstructionData;
};

export function parseOpenRevealWindowInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedOpenRevealWindowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      bid: getNextAccount(),
      permission: getNextAccount(),
      permissionProgram: getNextAccount(),
    },
    data: getOpenRevealWindowInstructionDataDecoder().decode(instruction.data),
  };
}
