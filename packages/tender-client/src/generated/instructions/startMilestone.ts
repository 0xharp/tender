/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const START_MILESTONE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [63, 104, 131, 234, 168, 124, 236, 168],
);

export function getStartMilestoneDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    START_MILESTONE_DISCRIMINATOR,
  );
}

export type StartMilestoneInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountRfp extends string | AccountMeta<string> = string,
  TAccountMilestone extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? WritableSignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountRfp extends string ? WritableAccount<TAccountRfp> : TAccountRfp,
      TAccountMilestone extends string
        ? WritableAccount<TAccountMilestone>
        : TAccountMilestone,
      ...TRemainingAccounts,
    ]
  >;

export type StartMilestoneInstructionData = {
  discriminator: ReadonlyUint8Array;
  milestoneIndex: number;
};

export type StartMilestoneInstructionDataArgs = { milestoneIndex: number };

export function getStartMilestoneInstructionDataEncoder(): FixedSizeEncoder<StartMilestoneInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["milestoneIndex", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: START_MILESTONE_DISCRIMINATOR }),
  );
}

export function getStartMilestoneInstructionDataDecoder(): FixedSizeDecoder<StartMilestoneInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["milestoneIndex", getU8Decoder()],
  ]);
}

export function getStartMilestoneInstructionDataCodec(): FixedSizeCodec<
  StartMilestoneInstructionDataArgs,
  StartMilestoneInstructionData
> {
  return combineCodec(
    getStartMilestoneInstructionDataEncoder(),
    getStartMilestoneInstructionDataDecoder(),
  );
}

export type StartMilestoneInput<
  TAccountProvider extends string = string,
  TAccountRfp extends string = string,
  TAccountMilestone extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  rfp: Address<TAccountRfp>;
  milestone: Address<TAccountMilestone>;
  milestoneIndex: StartMilestoneInstructionDataArgs["milestoneIndex"];
};

export function getStartMilestoneInstruction<
  TAccountProvider extends string,
  TAccountRfp extends string,
  TAccountMilestone extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: StartMilestoneInput<TAccountProvider, TAccountRfp, TAccountMilestone>,
  config?: { programAddress?: TProgramAddress },
): StartMilestoneInstruction<
  TProgramAddress,
  TAccountProvider,
  TAccountRfp,
  TAccountMilestone
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: true },
    rfp: { value: input.rfp ?? null, isWritable: true },
    milestone: { value: input.milestone ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("rfp", accounts.rfp),
      getAccountMeta("milestone", accounts.milestone),
    ],
    data: getStartMilestoneInstructionDataEncoder().encode(
      args as StartMilestoneInstructionDataArgs,
    ),
    programAddress,
  } as StartMilestoneInstruction<
    TProgramAddress,
    TAccountProvider,
    TAccountRfp,
    TAccountMilestone
  >);
}

export type ParsedStartMilestoneInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    rfp: TAccountMetas[1];
    milestone: TAccountMetas[2];
  };
  data: StartMilestoneInstructionData;
};

export function parseStartMilestoneInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStartMilestoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      provider: getNextAccount(),
      rfp: getNextAccount(),
      milestone: getNextAccount(),
    },
    data: getStartMilestoneInstructionDataDecoder().decode(instruction.data),
  };
}
