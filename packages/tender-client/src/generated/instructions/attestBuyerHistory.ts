/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findMainRepPda } from "../pdas";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const ATTEST_BUYER_HISTORY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([224, 93, 128, 184, 44, 219, 234, 223]);

export function getAttestBuyerHistoryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ATTEST_BUYER_HISTORY_DISCRIMINATOR,
  );
}

export type AttestBuyerHistoryInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMainWallet extends string | AccountMeta<string> = string,
  TAccountRfp extends string | AccountMeta<string> = string,
  TAccountEphemeralRep extends string | AccountMeta<string> = string,
  TAccountMainRep extends string | AccountMeta<string> = string,
  TAccountInstructionsSysvar extends string | AccountMeta<string> =
    "Sysvar1nstructions1111111111111111111111111",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMainWallet extends string
        ? WritableSignerAccount<TAccountMainWallet> &
            AccountSignerMeta<TAccountMainWallet>
        : TAccountMainWallet,
      TAccountRfp extends string ? WritableAccount<TAccountRfp> : TAccountRfp,
      TAccountEphemeralRep extends string
        ? ReadonlyAccount<TAccountEphemeralRep>
        : TAccountEphemeralRep,
      TAccountMainRep extends string
        ? WritableAccount<TAccountMainRep>
        : TAccountMainRep,
      TAccountInstructionsSysvar extends string
        ? ReadonlyAccount<TAccountInstructionsSysvar>
        : TAccountInstructionsSysvar,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AttestBuyerHistoryInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AttestBuyerHistoryInstructionDataArgs = {};

export function getAttestBuyerHistoryInstructionDataEncoder(): FixedSizeEncoder<AttestBuyerHistoryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ATTEST_BUYER_HISTORY_DISCRIMINATOR,
    }),
  );
}

export function getAttestBuyerHistoryInstructionDataDecoder(): FixedSizeDecoder<AttestBuyerHistoryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAttestBuyerHistoryInstructionDataCodec(): FixedSizeCodec<
  AttestBuyerHistoryInstructionDataArgs,
  AttestBuyerHistoryInstructionData
> {
  return combineCodec(
    getAttestBuyerHistoryInstructionDataEncoder(),
    getAttestBuyerHistoryInstructionDataDecoder(),
  );
}

export type AttestBuyerHistoryAsyncInput<
  TAccountMainWallet extends string = string,
  TAccountRfp extends string = string,
  TAccountEphemeralRep extends string = string,
  TAccountMainRep extends string = string,
  TAccountInstructionsSysvar extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The buyer's main wallet — pays tx fee + rent for `main_rep` if it
   * needs to be initialized, signs the tx envelope, AND signs the
   * binding message in the prepended Ed25519SigVerify ix.
   */
  mainWallet: TransactionSigner<TAccountMainWallet>;
  /**
   * The private RFP being attested. Must be `Private` + `Completed` +
   * not previously attested.
   */
  rfp: Address<TAccountRfp>;
  /**
   * The stranded per-RFP ephemeral rep account. Read-only — we just
   * snapshot its counters into `main_rep`. Anchor verifies the seed
   * derivation, ensuring this is the correct ephemeral's rep PDA.
   */
  ephemeralRep: Address<TAccountEphemeralRep>;
  /**
   * Main wallet's rep account. `init_if_needed` because the buyer may
   * have only ever run private RFPs before this attestation, in which
   * case their main rep PDA doesn't exist yet.
   */
  mainRep?: Address<TAccountMainRep>;
  /**
   * which validate its address. Required to introspect the
   * Ed25519SigVerify ix that proves `main_wallet → rfp.buyer` binding.
   */
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAttestBuyerHistoryInstructionAsync<
  TAccountMainWallet extends string,
  TAccountRfp extends string,
  TAccountEphemeralRep extends string,
  TAccountMainRep extends string,
  TAccountInstructionsSysvar extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: AttestBuyerHistoryAsyncInput<
    TAccountMainWallet,
    TAccountRfp,
    TAccountEphemeralRep,
    TAccountMainRep,
    TAccountInstructionsSysvar,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AttestBuyerHistoryInstruction<
    TProgramAddress,
    TAccountMainWallet,
    TAccountRfp,
    TAccountEphemeralRep,
    TAccountMainRep,
    TAccountInstructionsSysvar,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mainWallet: { value: input.mainWallet ?? null, isWritable: true },
    rfp: { value: input.rfp ?? null, isWritable: true },
    ephemeralRep: { value: input.ephemeralRep ?? null, isWritable: false },
    mainRep: { value: input.mainRep ?? null, isWritable: true },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.mainRep.value) {
    accounts.mainRep.value = await findMainRepPda({
      mainWallet: getAddressFromResolvedInstructionAccount(
        "mainWallet",
        accounts.mainWallet.value,
      ),
    });
  }
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("mainWallet", accounts.mainWallet),
      getAccountMeta("rfp", accounts.rfp),
      getAccountMeta("ephemeralRep", accounts.ephemeralRep),
      getAccountMeta("mainRep", accounts.mainRep),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAttestBuyerHistoryInstructionDataEncoder().encode({}),
    programAddress,
  } as AttestBuyerHistoryInstruction<
    TProgramAddress,
    TAccountMainWallet,
    TAccountRfp,
    TAccountEphemeralRep,
    TAccountMainRep,
    TAccountInstructionsSysvar,
    TAccountSystemProgram
  >);
}

export type AttestBuyerHistoryInput<
  TAccountMainWallet extends string = string,
  TAccountRfp extends string = string,
  TAccountEphemeralRep extends string = string,
  TAccountMainRep extends string = string,
  TAccountInstructionsSysvar extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * The buyer's main wallet — pays tx fee + rent for `main_rep` if it
   * needs to be initialized, signs the tx envelope, AND signs the
   * binding message in the prepended Ed25519SigVerify ix.
   */
  mainWallet: TransactionSigner<TAccountMainWallet>;
  /**
   * The private RFP being attested. Must be `Private` + `Completed` +
   * not previously attested.
   */
  rfp: Address<TAccountRfp>;
  /**
   * The stranded per-RFP ephemeral rep account. Read-only — we just
   * snapshot its counters into `main_rep`. Anchor verifies the seed
   * derivation, ensuring this is the correct ephemeral's rep PDA.
   */
  ephemeralRep: Address<TAccountEphemeralRep>;
  /**
   * Main wallet's rep account. `init_if_needed` because the buyer may
   * have only ever run private RFPs before this attestation, in which
   * case their main rep PDA doesn't exist yet.
   */
  mainRep: Address<TAccountMainRep>;
  /**
   * which validate its address. Required to introspect the
   * Ed25519SigVerify ix that proves `main_wallet → rfp.buyer` binding.
   */
  instructionsSysvar?: Address<TAccountInstructionsSysvar>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAttestBuyerHistoryInstruction<
  TAccountMainWallet extends string,
  TAccountRfp extends string,
  TAccountEphemeralRep extends string,
  TAccountMainRep extends string,
  TAccountInstructionsSysvar extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: AttestBuyerHistoryInput<
    TAccountMainWallet,
    TAccountRfp,
    TAccountEphemeralRep,
    TAccountMainRep,
    TAccountInstructionsSysvar,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AttestBuyerHistoryInstruction<
  TProgramAddress,
  TAccountMainWallet,
  TAccountRfp,
  TAccountEphemeralRep,
  TAccountMainRep,
  TAccountInstructionsSysvar,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    mainWallet: { value: input.mainWallet ?? null, isWritable: true },
    rfp: { value: input.rfp ?? null, isWritable: true },
    ephemeralRep: { value: input.ephemeralRep ?? null, isWritable: false },
    mainRep: { value: input.mainRep ?? null, isWritable: true },
    instructionsSysvar: {
      value: input.instructionsSysvar ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.instructionsSysvar.value) {
    accounts.instructionsSysvar.value =
      "Sysvar1nstructions1111111111111111111111111" as Address<"Sysvar1nstructions1111111111111111111111111">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("mainWallet", accounts.mainWallet),
      getAccountMeta("rfp", accounts.rfp),
      getAccountMeta("ephemeralRep", accounts.ephemeralRep),
      getAccountMeta("mainRep", accounts.mainRep),
      getAccountMeta("instructionsSysvar", accounts.instructionsSysvar),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAttestBuyerHistoryInstructionDataEncoder().encode({}),
    programAddress,
  } as AttestBuyerHistoryInstruction<
    TProgramAddress,
    TAccountMainWallet,
    TAccountRfp,
    TAccountEphemeralRep,
    TAccountMainRep,
    TAccountInstructionsSysvar,
    TAccountSystemProgram
  >);
}

export type ParsedAttestBuyerHistoryInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * The buyer's main wallet — pays tx fee + rent for `main_rep` if it
     * needs to be initialized, signs the tx envelope, AND signs the
     * binding message in the prepended Ed25519SigVerify ix.
     */
    mainWallet: TAccountMetas[0];
    /**
     * The private RFP being attested. Must be `Private` + `Completed` +
     * not previously attested.
     */
    rfp: TAccountMetas[1];
    /**
     * The stranded per-RFP ephemeral rep account. Read-only — we just
     * snapshot its counters into `main_rep`. Anchor verifies the seed
     * derivation, ensuring this is the correct ephemeral's rep PDA.
     */
    ephemeralRep: TAccountMetas[2];
    /**
     * Main wallet's rep account. `init_if_needed` because the buyer may
     * have only ever run private RFPs before this attestation, in which
     * case their main rep PDA doesn't exist yet.
     */
    mainRep: TAccountMetas[3];
    /**
     * which validate its address. Required to introspect the
     * Ed25519SigVerify ix that proves `main_wallet → rfp.buyer` binding.
     */
    instructionsSysvar: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: AttestBuyerHistoryInstructionData;
};

export function parseAttestBuyerHistoryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAttestBuyerHistoryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      mainWallet: getNextAccount(),
      rfp: getNextAccount(),
      ephemeralRep: getNextAccount(),
      mainRep: getNextAccount(),
      instructionsSysvar: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAttestBuyerHistoryInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
