/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_BID_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  185, 148, 101, 95, 131, 251, 233, 99,
]);

export function getFinalizeBidDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_BID_DISCRIMINATOR,
  );
}

export type FinalizeBidInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountProvider extends string | AccountMeta<string> = string,
  TAccountBid extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountProvider extends string
        ? ReadonlySignerAccount<TAccountProvider> &
            AccountSignerMeta<TAccountProvider>
        : TAccountProvider,
      TAccountBid extends string ? WritableAccount<TAccountBid> : TAccountBid,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeBidInstructionData = { discriminator: ReadonlyUint8Array };

export type FinalizeBidInstructionDataArgs = {};

export function getFinalizeBidInstructionDataEncoder(): FixedSizeEncoder<FinalizeBidInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_BID_DISCRIMINATOR }),
  );
}

export function getFinalizeBidInstructionDataDecoder(): FixedSizeDecoder<FinalizeBidInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeBidInstructionDataCodec(): FixedSizeCodec<
  FinalizeBidInstructionDataArgs,
  FinalizeBidInstructionData
> {
  return combineCodec(
    getFinalizeBidInstructionDataEncoder(),
    getFinalizeBidInstructionDataDecoder(),
  );
}

export type FinalizeBidInput<
  TAccountProvider extends string = string,
  TAccountBid extends string = string,
> = {
  provider: TransactionSigner<TAccountProvider>;
  bid: Address<TAccountBid>;
};

export function getFinalizeBidInstruction<
  TAccountProvider extends string,
  TAccountBid extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: FinalizeBidInput<TAccountProvider, TAccountBid>,
  config?: { programAddress?: TProgramAddress },
): FinalizeBidInstruction<TProgramAddress, TAccountProvider, TAccountBid> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    provider: { value: input.provider ?? null, isWritable: false },
    bid: { value: input.bid ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("provider", accounts.provider),
      getAccountMeta("bid", accounts.bid),
    ],
    data: getFinalizeBidInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeBidInstruction<TProgramAddress, TAccountProvider, TAccountBid>);
}

export type ParsedFinalizeBidInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    provider: TAccountMetas[0];
    bid: TAccountMetas[1];
  };
  data: FinalizeBidInstructionData;
};

export function parseFinalizeBidInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeBidInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { provider: getNextAccount(), bid: getNextAccount() },
    data: getFinalizeBidInstructionDataDecoder().decode(instruction.data),
  };
}
