/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const REVEAL_RESERVE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  35, 70, 113, 147, 101, 240, 175, 78,
]);

export function getRevealReserveDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REVEAL_RESERVE_DISCRIMINATOR,
  );
}

export type RevealReserveInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountRfp extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuyer extends string
        ? ReadonlySignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountRfp extends string ? WritableAccount<TAccountRfp> : TAccountRfp,
      ...TRemainingAccounts,
    ]
  >;

export type RevealReserveInstructionData = {
  discriminator: ReadonlyUint8Array;
  reserveAmount: bigint;
  reserveNonce: ReadonlyUint8Array;
};

export type RevealReserveInstructionDataArgs = {
  reserveAmount: number | bigint;
  reserveNonce: ReadonlyUint8Array;
};

export function getRevealReserveInstructionDataEncoder(): FixedSizeEncoder<RevealReserveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["reserveAmount", getU64Encoder()],
      ["reserveNonce", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: REVEAL_RESERVE_DISCRIMINATOR }),
  );
}

export function getRevealReserveInstructionDataDecoder(): FixedSizeDecoder<RevealReserveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["reserveAmount", getU64Decoder()],
    ["reserveNonce", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getRevealReserveInstructionDataCodec(): FixedSizeCodec<
  RevealReserveInstructionDataArgs,
  RevealReserveInstructionData
> {
  return combineCodec(
    getRevealReserveInstructionDataEncoder(),
    getRevealReserveInstructionDataDecoder(),
  );
}

export type RevealReserveInput<
  TAccountBuyer extends string = string,
  TAccountRfp extends string = string,
> = {
  buyer: TransactionSigner<TAccountBuyer>;
  rfp: Address<TAccountRfp>;
  reserveAmount: RevealReserveInstructionDataArgs["reserveAmount"];
  reserveNonce: RevealReserveInstructionDataArgs["reserveNonce"];
};

export function getRevealReserveInstruction<
  TAccountBuyer extends string,
  TAccountRfp extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: RevealReserveInput<TAccountBuyer, TAccountRfp>,
  config?: { programAddress?: TProgramAddress },
): RevealReserveInstruction<TProgramAddress, TAccountBuyer, TAccountRfp> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: false },
    rfp: { value: input.rfp ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("rfp", accounts.rfp),
    ],
    data: getRevealReserveInstructionDataEncoder().encode(
      args as RevealReserveInstructionDataArgs,
    ),
    programAddress,
  } as RevealReserveInstruction<TProgramAddress, TAccountBuyer, TAccountRfp>);
}

export type ParsedRevealReserveInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    buyer: TAccountMetas[0];
    rfp: TAccountMetas[1];
  };
  data: RevealReserveInstructionData;
};

export function parseRevealReserveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevealReserveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { buyer: getNextAccount(), rfp: getNextAccount() },
    data: getRevealReserveInstructionDataDecoder().decode(instruction.data),
  };
}
