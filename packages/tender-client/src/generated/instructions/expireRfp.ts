/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TENDER_PROGRAM_ADDRESS } from "../programs";

export const EXPIRE_RFP_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  118, 121, 90, 249, 175, 143, 240, 196,
]);

export function getExpireRfpDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXPIRE_RFP_DISCRIMINATOR);
}

export type ExpireRfpInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountRfp extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountRfp extends string ? WritableAccount<TAccountRfp> : TAccountRfp,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireRfpInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireRfpInstructionDataArgs = {};

export function getExpireRfpInstructionDataEncoder(): FixedSizeEncoder<ExpireRfpInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_RFP_DISCRIMINATOR }),
  );
}

export function getExpireRfpInstructionDataDecoder(): FixedSizeDecoder<ExpireRfpInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireRfpInstructionDataCodec(): FixedSizeCodec<
  ExpireRfpInstructionDataArgs,
  ExpireRfpInstructionData
> {
  return combineCodec(
    getExpireRfpInstructionDataEncoder(),
    getExpireRfpInstructionDataDecoder(),
  );
}

export type ExpireRfpInput<
  TAccountCaller extends string = string,
  TAccountRfp extends string = string,
> = {
  /**
   * Permissionless caller - typically the buyer (in their /me/projects
   * "Action required" surface) or any provider with a stuck bid.
   */
  caller: TransactionSigner<TAccountCaller>;
  rfp: Address<TAccountRfp>;
};

export function getExpireRfpInstruction<
  TAccountCaller extends string,
  TAccountRfp extends string,
  TProgramAddress extends Address = typeof TENDER_PROGRAM_ADDRESS,
>(
  input: ExpireRfpInput<TAccountCaller, TAccountRfp>,
  config?: { programAddress?: TProgramAddress },
): ExpireRfpInstruction<TProgramAddress, TAccountCaller, TAccountRfp> {
  // Program address.
  const programAddress = config?.programAddress ?? TENDER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: false },
    rfp: { value: input.rfp ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("rfp", accounts.rfp),
    ],
    data: getExpireRfpInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireRfpInstruction<TProgramAddress, TAccountCaller, TAccountRfp>);
}

export type ParsedExpireRfpInstruction<
  TProgram extends string = typeof TENDER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Permissionless caller - typically the buyer (in their /me/projects
     * "Action required" surface) or any provider with a stuck bid.
     */
    caller: TAccountMetas[0];
    rfp: TAccountMetas[1];
  };
  data: ExpireRfpInstructionData;
};

export function parseExpireRfpInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireRfpInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { caller: getNextAccount(), rfp: getNextAccount() },
    data: getExpireRfpInstructionDataDecoder().decode(instruction.data),
  };
}
