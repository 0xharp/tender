/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ATTEST_WIN_RECEIPT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([104, 133, 184, 177, 177, 242, 240, 241]);

export function getAttestWinReceiptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ATTEST_WIN_RECEIPT_DISCRIMINATOR,
  );
}

export type AttestWinReceipt = {
  discriminator: ReadonlyUint8Array;
  bid: Address;
  mainWallet: Address;
  at: bigint;
  bump: number;
};

export type AttestWinReceiptArgs = {
  bid: Address;
  mainWallet: Address;
  at: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link AttestWinReceiptArgs} account data. */
export function getAttestWinReceiptEncoder(): FixedSizeEncoder<AttestWinReceiptArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["bid", getAddressEncoder()],
      ["mainWallet", getAddressEncoder()],
      ["at", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ATTEST_WIN_RECEIPT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link AttestWinReceipt} account data. */
export function getAttestWinReceiptDecoder(): FixedSizeDecoder<AttestWinReceipt> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["bid", getAddressDecoder()],
    ["mainWallet", getAddressDecoder()],
    ["at", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link AttestWinReceipt} account data. */
export function getAttestWinReceiptCodec(): FixedSizeCodec<
  AttestWinReceiptArgs,
  AttestWinReceipt
> {
  return combineCodec(
    getAttestWinReceiptEncoder(),
    getAttestWinReceiptDecoder(),
  );
}

export function decodeAttestWinReceipt<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<AttestWinReceipt, TAddress>;
export function decodeAttestWinReceipt<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<AttestWinReceipt, TAddress>;
export function decodeAttestWinReceipt<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<AttestWinReceipt, TAddress>
  | MaybeAccount<AttestWinReceipt, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getAttestWinReceiptDecoder(),
  );
}

export async function fetchAttestWinReceipt<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<AttestWinReceipt, TAddress>> {
  const maybeAccount = await fetchMaybeAttestWinReceipt(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeAttestWinReceipt<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<AttestWinReceipt, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeAttestWinReceipt(maybeAccount);
}

export async function fetchAllAttestWinReceipt(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<AttestWinReceipt>[]> {
  const maybeAccounts = await fetchAllMaybeAttestWinReceipt(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeAttestWinReceipt(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<AttestWinReceipt>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeAttestWinReceipt(maybeAccount),
  );
}

export function getAttestWinReceiptSize(): number {
  return 81;
}
